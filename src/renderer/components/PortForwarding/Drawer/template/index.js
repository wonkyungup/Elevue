import directTitle from './Direction/Title'
import directContext from './Direction/Content'
import firstStepTitle from './FirstStep/Title'
import firstStepContext from './FirstStep/Content'
import secondStepTitle from './SecondStep/Title'
import secondStepContext from './SecondStep/Content'

export {
    directTitle,
    directContext,
    firstStepTitle,
    firstStepContext,
    secondStepTitle,
    secondStepContext
}
